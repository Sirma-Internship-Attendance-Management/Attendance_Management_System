<Window x:Class="Attendance_Management_System.Views.CompanyAdminView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Attendance_Management_System.Views"
        mc:Ignorable="d"
        Title="Company Administration" Height="450" Width="800">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Employee Management" FontSize="24" FontWeight="Bold" Margin="0 0 0 10" />
            <DataGrid ItemsSource="{Binding Employees}" AutoGenerateColumns="False" Margin="0 0 0 10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Position" Binding="{Binding Position}" Width="*"/>
                    <DataGridTextColumn Header="Contact Details" Binding="{Binding ContactDetails}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Orientation="Horizontal">
                <Button Content="Add Employee" Command="{Binding AddEmployeeCommand}" Margin="0 0 10 0"/>
                <Button Content="Edit Employee" Command="{Binding EditEmployeeCommand}" Margin="0 0 10 0"/>
                <Button Content="Delete Employee" Command="{Binding DeleteEmployeeCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
